/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/proyectos/sumak-nature",
  assetPrefix: "/proyectos/sumak-nature/",
  reactStrictMode: true,
};

module.exports = nextConfig;
